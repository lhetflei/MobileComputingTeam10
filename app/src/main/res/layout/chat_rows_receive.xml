<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:textColor = "?android:textColorPrimary">

    <TextView
        android:id="@+id/message_rows_receive"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:maxWidth="280dp"
        android:padding="16dp"
        android:text="Das ist eine Beispielnachricht, die endlos lang gehen soll außer"
        android:textSize="18sp"
        app:layout_constraintStart_toEndOf="@+id/message_rows_receive_imageview"
        app:layout_constraintTop_toTopOf="@+id/message_rows_receive_imageview"
        android:background="@drawable/customborder"/>

    <ImageView
        android:id="@+id/message_rows_receive_imageview"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/googleg_standard_color_18" />

</androidx.constraintlayout.widget.ConstraintLayout>